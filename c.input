#include <stdio.h>

int main(){
    float f = 3.5;
    int i =100;
    char c = 'c';

    if (i == 100) {
        return 0;
        printf("%f; %f; %f; \n", f,i,c);
        printf("%d; %d; %d; \n", f,i,c);
        printf("%c; %c; %c; \n", f,i,c);

    }
}